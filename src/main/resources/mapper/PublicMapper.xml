<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.iandtop.dao.PublicDAO">

    <select id="retrieveBySql" resultType="java.util.Map" parameterType="java.lang.String" >
        ${_parameter}
    </select>

    <update id="createTable" parameterType="java.lang.String" >
        ${_parameter}
    </update>

    <update id="insert" parameterType="java.lang.String" >
        ${_parameter}
    </update>

    <select id="intfindBysql" parameterType="java.lang.String" resultType="java.lang.Integer">
        ${_parameter}
    </select>
</mapper>

